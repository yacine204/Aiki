In mathematics, an infinite sequence of numbers s 0, s 1, s 2, s 3, … s_₀,s_₁,s_₂,s_₃,… is called constant-recursive if it satisfies an equation of the form s n = c 1 s n − 1 + c 2 s n − 2 + ⋯ + c d s n − d, s_n=c_₁s_n-1+c_₂s_n-2+dots +c_ds_n-d, for all n ≥ d n≥ d, where c i c_i are constants. The equation is called a linear recurrence relation.
The concept is also known as a linear recurrence sequence, linear-recursive sequence, linear-recurrent sequence, or a C-finite sequence.
For example, the Fibonacci sequence 0, 1, 2, 3, 5, 8, 13, … 0,1,2,3,5,8,13,…,
is constant-recursive because it satisfies the linear recurrence F n = F n − 1 + F n − 2 F_n=F_n-1+F_n-2: each number in the sequence is the sum of the previous two.
Other examples include the power of two sequence 1, 2, 4, 8, 16, … 1,2,4,8,16,…, where each number is the sum of twice the previous number, and the square number sequence 0, 1, 4, 9, 16, 25, … 0,1,4,9,16,25,…. All arithmetic progressions, all geometric progressions, and all polynomials are constant-recursive. However, not all sequences are constant-recursive; for example, the factorial sequence 1, 2, 6, 24, 120, … 1,2,6,24,120,… is not constant-recursive.
Constant-recursive sequences are studied in combinatorics and the theory of finite differences. They also arise in algebraic number theory, due to the relation of the sequence to polynomial roots; in the analysis of algorithms, as the running time of simple recursive functions; and in the theory of formal languages, where they count strings up to a given length in a regular language. Constant-recursive sequences are closed under important mathematical operations such as term-wise addition, term-wise multiplication, and Cauchy product.
The Skolem–Mahler–Lech theorem states that the zeros of a constant-recursive sequence have a regularly repeating (eventually periodic) form. The Skolem problem, which asks for an algorithm to determine whether a linear recurrence has at least one zero, is an unsolved problem in mathematics. == Definition == A constant-recursive sequence is any sequence of integers, rational numbers, algebraic numbers, real numbers, or complex numbers s 0, s 1, s 2, s 3, … s_₀,s_₁,s_₂,s_₃,… (written as (s n) n = 0 ∞ (s_n)_n=0^∈fty as a shorthand) satisfying a formula of the form s n = c 1 s n − 1 + c 2 s n − 2 + ⋯ + c d s n − d = ∑ k = 1 d c k s n − k, s_n=c_₁s_n-1+c_₂s_n-2+dots +c_ds_n-d=∑ _k=1^dc_ks_n-k, for all n ≥ d, n≥ d, for some fixed coefficients c 1, c 2, …, c d c_₁,c_₂,dots,c_d ranging over the same domain as the sequence (integers, rational numbers, algebraic numbers, real numbers, or complex numbers).
The equation is called a linear recurrence with constant coefficients of order d.
The order of the sequence is the smallest positive integer d such that the sequence satisfies a recurrence of order d, or d = 0 d=0 for the everywhere-zero sequence.
The definition above allows eventually-periodic sequences such as 1, 0, … 1,0,… and 0, 1, 0, … 0,1,0,…. Some authors require that c d ≠ 0 c_d≠ 0, which excludes such sequences. == Examples == = == Fibonacci and Lucas sequences == =
The sequence 0, 1, 2, 3, 5, 8, 13,... of Fibonacci numbers is constant-recursive of order 2 because it satisfies the recurrence F n = F n − 1 + F n − 2 F_n=F_n-1+F_n-2 with F 0 = 0, F 1 = 1 F_₀=0,F_₁=1. For example, F 2 = F 1 + F 0 = 1 + 0 = 1 F_₂=F_₁+F_₀=1+0=1 and F 6 = F 5 + F 4 = 5 + 3 = 8 F_₆=F_₅+F_₄=5+3=8. The sequence 2, 1, 3, 4, 7, 1,... of Lucas numbers satisfies the same recurrence as the Fibonacci sequence but with initial conditions L 0 = 2 L_₀=2 and L 1 = 1 L_₁=1. More generally, every Lucas sequence is constant-recursive of order 2. = == Arithmetic progressions == =
For any a and any r ≠ 0 r≠ 0, the arithmetic progression a + r, a + 2 r, … a+r,a+2r,… is constant-recursive of order 2, because it satisfies s n = 2 s n − 1 − s n − 2 s_n=2s_n-1-s_n-2. Generalizing this, see polynomial sequences below. = == Geometric progressions == =
For any a ≠ 0 a≠ 0 and r, the geometric progression a r, a r 2, … a,ar,ar²,… is constant-recursive of order 1, because it satisfies s n = r s n − 1 s_n=rs_n-1. This includes, for example, the sequence 1, 2, 4, 8, 16,... as well as the rational number sequence 1 2, 1 4, 1 8, 1 16,... textstyle 1,frac 12,frac 14,frac 18,frac 116,.... = == Eventually periodic sequences == =
A sequence that is eventually periodic with period length ℓ ell is constant-recursive, since it satisfies s n = s n − ℓ s_n=s_n-ell for all n ≥ d n≥ d, where the order d is the length of the initial segment including the first repeating block. Examples of such sequences are 1, 0,... (order 1) and 1, 6,... (order 2). = == Polynomial sequences == =
A sequence defined by a polynomial s n = a 0 + a 1 n + a 2 n 2 + ⋯ + a d n d s_n=a_₀+a_₁n+a_₂n²+⋯ +a_dn^d is constant-recursive. The sequence satisfies a recurrence of order d + 1 d+1 (where d is the degree of the polynomial), with coefficients given by the corresponding element of the binomial transform. The first few such equations are s n = 1 ⋅ s n − 1 s_n=1· s_n-1 for a degree 0 (that is, constant) polynomial, s n = 2 ⋅ s n − 1 − 1 ⋅ s n − 2 s_n=2· s_n-1-1· s_n-2 for a degree 1 or less polynomial, s n = 3 ⋅ s n − 1 − 3 ⋅ s n − 2 + 1 ⋅ s n − 3 s_n=3· s_n-1-3· s_n-2+1· s_n-3 for a degree 2 or less polynomial, and s n = 4 ⋅ s n − 1 − 6 ⋅ s n − 2 + 4 ⋅ s n − 3 − 1 ⋅ s n − 4 s_n=4· s_n-1-6· s_n-2+4· s_n-3-1· s_n-4 for a degree 3 or less polynomial.
A sequence obeying the order-d equation also obeys all higher order equations. These identities may be proved in a number of ways, including via the theory of finite differences.
Any sequence of d + 1 d+1 integer, real, or complex values can be used as initial conditions for a constant-recursive sequence of order d + 1 d+1. If the initial conditions lie on a polynomial of degree d − 1 d-1 or less, then the constant-recursive sequence also obeys a lower order equation. = == Enumeration of words in a regular language == =
Let L be a regular language, and let s n s_n be the number of words of length n in L. Then (s n) n = 0 ∞ (s_n)_n=0^∈fty is constant-recursive. For example, s n = 2 n s_n=2^n for the language of all binary strings, s n = 1 s_n=1 for the language of all unary strings, and s n = F n + 2 s_n=F_n+2 for the language of all binary strings that do not have two consecutive ones. More generally, any function accepted by a weighted automaton over the unary alphabet Σ = a Sigma =a over the semiring (R, +, ×) (ℝ,+,×) (which is in fact a ring, and even a field) is constant-recursive. = == Other examples == =
The sequences of Jacobsthal numbers, Padovan numbers, Pell numbers, and Perrin numbers are constant-recursive. = == Non-examples == =
The factorial sequence 1, 2, 6, 24, 120, 720, … 1,2,6,24,120,720,… is not constant-recursive. More generally, every constant-recursive function is asymptotically bounded by an exponential function (see #Closed-form characterization) and the factorial sequence grows faster than this.
The Catalan sequence 1, 2, 5, 14, 42, 132, … 1,2,5,14,42,132,… is not constant-recursive. This is because the generating function of the Catalan numbers is not a rational function (see #Equivalent definitions). == Equivalent definitions == = == In terms of matrices == = A sequence (s n) n = 0 ∞ (s_n)_n=0^∈fty is constant-recursive of order less than or equal to d if and only if it can be written as s n = u A n v s_n=uA^nv where u is a 1 × d 1× d vector, A is a d × d× d matrix, and v is a d × 1 d× 1 vector, where the elements come from the same domain (integers, rational numbers, algebraic numbers, real numbers, or complex numbers) as the original sequence. Specifically, v can be taken to be the first d values of the sequence, A the linear transformation that computes s n + 1, s n + 2, …, s n + d s_n+1,s_n+2,…,s_n+d from s n, s n + 1, …, s n + d − 1 s_n+1,…,s_n+d-1, and u the vector [ 0, …, 0, 1 ] [0,…,0,1]. = == In terms of non-homogeneous linear recurrences == = A non-homogeneous linear recurrence is an equation of the form s n = c 1 s n − 1 + c 2 s n − 2 + ⋯ + c d s n − d + c s_n=c_₁s_n-1+c_₂s_n-2+dots +c_ds_n-d+c where c is an additional constant. Any sequence satisfying a non-homogeneous linear recurrence is constant-recursive. This is because subtracting the equation for s n − 1 s_n-1 from the equation for s n s_n yields a homogeneous recurrence for s n − s n − 1 s_n-s_n-1, from which we can solve for s n s_n to obtain s n = (c 1 + 1) s n − 1 + (c 2 − c 1) s n − 2 + ⋯ + (c d − c d − 1) s n − d − c d s n − d − 1. beginaligneds_n=&(c_₁+1)s_n-1&+(c_₂-c_₁)s_n-2+dots +(c_d-c_d-1)s_n-d&-c_ds_n-d-1.endaligned = == In terms of generating functions == = A sequence is constant-recursive precisely when its generating function ∑ n = 0 ∞ s n x n = s 0 + s 1 x 1 + s 2 x 2 + s 3 x 3 + ⋯ ∑ _n=0^∈fty s_nx^n=s_₀+s_₁x¹+s_₂x²+s_₃x³+⋯ is a rational function p (x) / q (x) p(x)/q(x), where p and q are polynomials and q (0) = 1 q(0)=1.
Moreover, the order of the sequence is the minimum d such that it has such a form with deg q (x) ≤ d textdeg q(x)≤ d and deg p (x) < d textdeg p(x)<d.
The denominator is the polynomial obtained from the auxiliary polynomial by reversing the order of the coefficients, and the numerator is determined by the initial values of the sequence: ∑ n = 0 ∞ s n x n = b 0 + b 1 x 1 + b 2 x 2 + ⋯ + b d − 1 x d − 1 − c 1 x 1 − c 2 x 2 − ⋯ − c d x d, ∑ _n=0^∈fty s_nx^n=frac b_₀+b_₁x¹+b_₂x²+dots +b_d-1x^d-1-c_₁x¹-c_₂x²-dots -c_dx^d, where b n = s n − c 1 s n − 1 − c 2 s n − 2 − ⋯ − c d s n − d. b_n=s_n-c_₁s_n-1-c_₂s_n-2-dots -c_ds_n-d. It follows from the above that the denominator q (x) q(x) must be a polynomial not divisible by x (and in particular nonzero). = == In terms of sequence spaces == = A sequence (s n) n = 0 ∞ (s_n)_n=0^∈fty is constant-recursive if and only if the set of sequences (s n + r) n = 0 ∞: r ≥ 0 (s_n+r)_n=0^∈fty:r≥ 0 is contained in a sequence space (vector space of sequences) whose dimension is finite. That is, (s n) n = 0 ∞ (s_n)_n=0^∈fty is contained in a finite-dimensional subspace of C N ℂ ^ℕ closed under the left-shift operator.
This characterization is because the order- d linear recurrence relation can be understood as a proof of linear dependence between the sequences (s n + r) n = 0 ∞ (s_n+r)_n=0^∈fty for r = 0, …, d r=0,…,d. An extension of this argument shows that the order of the sequence is equal to the dimension of the sequence space generated by (s n + r) n = 0 ∞ (s_n+r)_n=0^∈fty for all r. == Closed-form characterization == Constant-recursive sequences admit the following unique closed form characterization using exponential polynomials: every constant-recursive sequence can be written in the form s n = z n + k 1 (n) r 1 n + k 2 (n) r 2 n + ⋯ + k e (n) r e n, s_n=z_n+k_₁(n)r_₁^n+k_₂(n)r_₂^n+⋯ +k_e(n)r_e^n, for all n ≥ 0 n≥ 0, where The term z n z_n is a sequence which is zero for all n ≥ d n≥ d (where d is the order of the sequence);
The terms k 1 (n), k 2 (n), …, k e (n) k_₁(n),k_₂(n),…,k_e(n) are complex polynomials; and
The terms r 1, r 2, …, r k r_₁,r_₂,…,r_k are distinct complex constants.
This characterization is exact: every sequence of complex numbers that can be written in the above form is constant-recursive.
For example, the Fibonacci number F n F_n is written in this form using Binet's formula: F n = 1 5 φ n − 1 5 ψ n, F_n=frac 1sqrt 5varphi ^n-frac 1sqrt 5psi ^n, where φ = (1 + 5) / 2 ≈
1.61803
… varphi =(1+sqrt 5)/2approx 1.61803… is the golden ratio and ψ = − 1 / φ psi =-1/varphi. These are the roots of the equation x 2 − x − 1 = 0 x²⁻x-1=0. In this case, e = 2 e=2, z n = 0 z_n=0 for all n, k 1 (n) = k 2 (n) = 1 / 5 k_₁(n)=k_₂(n)=1/sqrt 5 are both constant polynomials, r 1 = φ r_₁=varphi, and r 2 = ψ r_₂=psi.
The term z n z_n is only needed when c d ≠ 0 c_d≠ 0; if c d = 0 c_d=0 then it corrects for the fact that some initial values may be exceptions to the general recurrence. In particular, z n = 0 z_n=0 for all n ≥ d n≥ d.
The complex numbers r 1, …, r n r_₁,…,r_n are the roots of the characteristic polynomial of the recurrence: x d − c 1 x d − 1 − ⋯ − c d − 1 x − c d x^d-c_₁x^d-1-dots -c_d-1x-c_d whose coefficients are the same as those of the recurrence.
We call r 1, …, r n r_₁,…,r_n the characteristic roots of the recurrence. If the sequence consists of integers or rational numbers, the roots will be algebraic numbers.
If the d roots r 1, r 2, …, r d r_₁,r_₂,dots,r_d are all distinct, then the polynomials k i (n) k_i(n) are all constants, which can be determined from the initial values of the sequence.
If the roots of the characteristic polynomial are not distinct, and r i r_i is a root of multiplicity m, then k i (n) k_i(n) in the formula has degree m − 1 m-1. For instance, if the characteristic polynomial factors as (x − r) 3 (x-r)³, with the same root r occurring three times, then the n th term is of the form s n = (a + b n + c n 2) r n. s_n=(a+bn+cn²)r^n. == Closure properties == = == Examples == =
The sum of two constant-recursive sequences is also constant-recursive. For example, the sum of s n = 2 n s_n=2^n and t n = n t_n=n is u n = 2 n + n u_n=2^n+n (1, 3, 6, 1, 20, … 1,3,6,1,20,…), which satisfies the recurrence u n = 4 u n − 1 − 5 u n − 2 + 2 u n − 3 u_n=4u_n-1-5u_n-2+2u_n-3. The new recurrence can be found by adding the generating functions for each sequence.
Similarly, the product of two constant-recursive sequences is constant-recursive. For example, the product of s n = 2 n s_n=2^n and t n = n t_n=n is u n = n ⋅ 2 n u_n=n· 2^n (0, 2, 8, 24, 64, … 0,2,8,24,64,…), which satisfies the recurrence u n = 4 u n − 1 − 4 u n − 2 u_n=4u_n-1-4u_n-2.
The left-shift sequence u n = s n + 1 u_n=s_n+1 and the right-shift sequence u n = s n − 1 u_n=s_n-1 (with u 0 = 0 u_₀=0) are constant-recursive because they satisfy the same recurrence relation. For example, because s n = 2 n s_n=2^n is constant-recursive, so is u n = 2 n + 1 u_n=2^n+1. = == List of operations == =
In general, constant-recursive sequences are closed under the following operations, where s = (s n) n ∈ N, t = (t n) n ∈ N s=(s_n)_n∈ ℕ,t=(t_n)_n∈ ℕ denote constant-recursive sequences, f (x), g (x) f(x),g(x) are their generating functions, and d, e d,e are their orders, respectively. The closure under term-wise addition and multiplication follows from the closed-form characterization in terms of exponential polynomials. The closure under Cauchy product follows from the generating function characterization. The requirement s 0 = 1 s_₀=1 for Cauchy inverse is necessary for the case of integer sequences, but can be replaced by s 0 ≠ 0 s_₀≠ 0 if the sequence is over any field (rational, algebraic, real, or complex numbers). == Behavior == = == Zeros == =
Despite satisfying a simple local formula, a constant-recursive sequence can exhibit complicated global behavior. Define a zero of a constant-recursive sequence to be a nonnegative integer n such that s n = 0 s_n=0. The Skolem–Mahler–Lech theorem states that the zeros of the sequence are eventually repeating: there exists constants M and N such that for all n > M n>M, s n = 0 s_n=0 if and only if s n + N = 0 s_n+N=0. This result holds for a constant-recursive sequence over the complex numbers, or more generally, over any field of characteristic zero. = == Decision problems == =
The pattern of zeros in a constant-recursive sequence can also be investigated from the perspective of computability theory. To do so, the description of the sequence s n s_n must be given a finite description; this can be done if the sequence is over the integers, rational numbers, or algebraic numbers.
Given such an encoding for sequences s n s_n, the following problems can be studied: Because the square of a constant-recursive sequence s n 2 s_n² is still constant-recursive (see closure properties), the existence-of-a-zero problem in the table above reduces to positivity, and infinitely-many-zeros reduces to eventual positivity. Other problems also reduce to those in the above table: for example, whether s n = c s_n=c for some n reduces to existence-of-a-zero for the sequence s n − c s_n-c. As a second example, for sequences in the real numbers, weak positivity (is s n ≥ 0 s_n≥ 0 for all n ?) reduces to positivity of the sequence − s n -s_n (because the answer must be negated, this is a Turing reduction).
The Skolem-Mahler-Lech theorem would provide answers to some of these questions, except that its proof is non-constructive. It states that for all n > M n>M, the zeros are repeating; however, the value of M is not known to be computable, so this does not lead to a solution to the existence-of-a-zero problem. On the other hand, the exact pattern which repeats after n > M n>M is computable. This is why the infinitely-many-zeros problem is decidable: just determine if the infinitely-repeating pattern is empty.
Decidability results are known when the order of a sequence is restricted to be small. For example, the Skolem problem is decidable for algebraic sequences of order up to 4. It is also known to be decidable for reversible integer sequences up to order 7, that is, sequences that may be continued backwards in the integers.
Decidability results are also known under the assumption of certain unproven conjectures in number theory. For example, decidability is known for rational sequences of order up to 5 subject to a conjecture known as Skolem's conjecture or the exponential local-global principle. Decidability is also known for all simple rational sequences (those with simple characteristic polynomial) subject to the Skolem conjecture and the weak p-adic Schanuel conjecture. = == Degeneracy == =
Let r 1, …, r n r_₁,…,r_n be the characteristic roots of a constant recursive sequence s. We say that the sequence is degenerate if the ratio r i / r j r_i/r_j is a root of unity, for any i ≠ j i≠ j. It is often easier to study non-degenerate sequences, and one can reduce to this using the following theorem: if s has order d and is contained in a number field K of degree k over Q mathbb Q, then there is a constant M (k, d) ≤ exp (2 d (3 log d) 1 / 2) if k = 1, 2 k d + 1 if k ≥ 2 M(k,d)≤ begincasesexp(2d(3log d)¹/²)&if k=1,2^kd+1&if k≥ 2endcases such that for some M ≤ M (k, d) M≤ M(k,d) each subsequence s M n + ℓ s_Mn+ell is either identically zero or non-degenerate. == Generalizations == A D-finite or holonomic sequence is a natural generalization where the coefficients of the recurrence are allowed to be polynomial functions of n rather than constants.
A k -regular sequence satisfies a linear recurrences with constant coefficients, but the recurrences take a different form. Rather than s n s_n being a linear combination of s m s_m for some integers m that are close to n, each term s n s_n in a k -regular sequence is a linear combination of s m s_m for some integers m whose base- k representations are close to that of n. Constant-recursive sequences can be thought of as 1 -regular sequences, where the base-1 representation of n consists of n copies of the digit 1. == Notes == == References == == External links == "OEIS Index Rec". OEIS index to a few thousand examples of linear recurrences, sorted by order (number of terms) and signature (vector of values of the constant coefficients)